import { testProp, fc } from 'ava-fast-check'

/**
 * Library under test
 */

import { decodeEnvironment } from '../../src/decode-environment'

testProp.skip(
    'TODO: property-test decode-environment',
    [
        // arbitraries
    ],
    (
        // test arguments
    ) => {
        // return assertion
    }, {
        verbose: true,
        numRuns: 100
    }
)
